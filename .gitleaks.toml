[extend]
useDefault = true

[[allowlists]]
description = "REPLACE_ME placeholders in the committed env templates"
targetRules = ["generic-api-key"]
paths = ['''\.env.*\.example$''']
regexes = ['''REPLACE_ME[A-Za-z0-9_]*''']
